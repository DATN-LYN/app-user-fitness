#import '../fragment/meta_fragment.graphql'
#import '../fragment/inbox_fragment.graphql'

query GetMyInboxes($input: QueryFilterDto!) {
  getMyInboxes(input: $input) {
    meta {
      ...Meta
    }
    items {
      ...Inbox
    }
  }
}
